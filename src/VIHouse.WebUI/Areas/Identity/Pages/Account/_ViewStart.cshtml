@{
    // Wraps every Account/*.cshtml page (Login, Register, ForgotPassword, etc.) with the
    // .auth-page baseline styling in _Layout.cshtml below. Account/Manage/_ViewStart.cshtml
    // overrides this for pages under Manage/, which get the sidebar shell instead.
    Layout = "_Layout";
}
