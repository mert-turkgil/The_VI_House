@model List<ExperienceCardViewModel>
@{
    // Same reasoning as _Upcoming.cshtml, at this strip's own per-view count of 3.
    const int perView = 3;
    var useCarousel = Model.Count >= perView;
}

@if (Model.Count > 0)
{
    <section class="home-section home-section--tinted" data-reveal data-swiper-scope>
        <div class="container home-section__header">
            <h2>@Loc["Home.Signature.Heading"]</h2>
            <div class="home-section__tools">
                @if (useCarousel)
                {
                    <partial name="_CarouselControls" model="@((Loc["Home.Hero.Previous"].Value, Loc["Home.Hero.Next"].Value))" />
                }
                <a class="home-section__viewall" href="/experiences">@Loc["Home.ViewAll"] &rarr;</a>
            </div>
        </div>
        <div class="container">
            @if (useCarousel)
            {
                <div class="swiper home-carousel" data-swiper data-swiper-per-view="@perView">
                    <div class="swiper-wrapper">
                        @foreach (var e in Model)
                        {
                            <div class="swiper-slide">
                                <partial name="_SignatureCard" model="e" />
                            </div>
                        }
                    </div>
                </div>
            }
            else
            {
                <div class="home-signature__grid">
                    @foreach (var e in Model)
                    {
                        <partial name="_SignatureCard" model="e" />
                    }
                </div>
            }
        </div>
    </section>
}
