@model List<ExperienceCardViewModel>
@{
    ViewData["Title"] = "Request Access";
}

<div class="apply-header">
    <div class="container">
        <span class="apply-header__eyebrow">Request Access</span>
        <h1>Choose an Experience</h1>
        <p>Pick the room you'd like to apply for — every application is reviewed by hand.</p>
    </div>
</div>

<div class="container">
    @if (Model.Count == 0)
    {
        <p style="text-align:center; padding: 48px 0;">Applications aren't open right now — check back soon.</p>
    }
    else
    {
        <div class="experience-grid">
            @foreach (var e in Model)
            {
                @* Same card as the public listing, pointed at the application funnel instead of
                   the detail page. This used to be a near-identical copy of that markup. *@
                e.Href = Url.Action("Index", "Application", new { area = "", experience = e.Slug });
                <partial name="/Views/Experiences/_ExperienceCard.cshtml" model="e" />
            }
        </div>
    }
</div>
