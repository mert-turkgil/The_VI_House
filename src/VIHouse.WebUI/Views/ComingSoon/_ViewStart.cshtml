@{
    // The curtain carries its own chrome — see _ComingSoonLayout. Set here rather than per-view so
    // that a second page behind the curtain (a thank-you screen, say) cannot forget it and silently
    // render inside the full site nav, every link of which redirects back here.
    Layout = "_ComingSoonLayout";
}
